# 먀로또

`assets/Frame2.png`를 사용하는 정적 웹 추첨기입니다. PNG 프레임 뒤쪽의 입 위치에 슬롯머신형 릴을 렌더링하며 GitHub Pages에서 별도 빌드 없이 실행됩니다.

## 화면 구성

오른쪽 컨트롤 영역은 두 탭으로 분리됩니다.

- `자동 번호 발급`: 무작위 번호를 1회 또는 지정 횟수만큼 연속 자동 발급하고 기록
- `추첨기`: 실제 추첨처럼 모든 릴을 먼저 회전시킨 뒤 사용자가 한 번 누를 때마다 그 순간 새로운 번호를 하나씩 무작위 추첨해 공개

## 자동 번호 발급

### 기본

- 번호 범위와 발급 개수를 지정
- 이미지 클릭 또는 `Space` 1회: 모든 릴 회전 시작
- 다시 클릭 또는 `Space`: 왼쪽부터 순차 감속/확정
- 결과는 자동 발급 기록에 저장

### 연속 자동

- 연속 실행 횟수를 `1~100회`로 지정
- 이미지 클릭 또는 `Space` 1회로 시작
- 기본 자동 번호 발급을 지정 횟수만큼 반복
- 각 회차 결과를 자동 발급 기록에 저장
- 실행 중 다시 누르면 현재 회차를 마친 뒤 중지

### 공통 설정

- 번호 범위 설정 가능
- 발급 개수 `1~7` 설정 가능
- 중복은 항상 비허용
- 번호 범위, 발급 개수, 발급 방식, 연속 횟수는 `localStorage`에 저장
- Web Crypto API 기반 번호 생성
- 최근 200회 자동 기록
- 현재 결과 / 개별 기록 / 전체 기록 복사 시 번호만 복사
- `전체 복사`는 한 줄에 한 조합 형식
- 날짜와 시간은 저장·표시·복사하지 않음

## 추첨기

추첨기 탭은 미리 정해 둔 번호를 순서대로 보여주는 방식이 아니라 **실시간 무작위 추첨 방식**입니다.

- 번호 범위와 추첨 개수를 별도로 지정
- 추첨 개수 `1~7` 지원
- 중복은 항상 비허용
- 추첨기 설정값은 `localStorage`에 저장
- 보너스 번호 기능 없음
- 첫 번째 이미지 클릭 또는 `Space`: 지정된 개수의 릴을 모두 회전 시작
- 이후 입력 1회마다 왼쪽 릴 하나만 정지
- 정지할 번호는 회전 시작 시 미리 만들지 않고 **해당 릴을 멈추는 입력이 발생한 순간 Web Crypto API로 새로 추첨**
- 이미 나온 번호는 후보에서 제외하므로 한 추첨 안에서 중복되지 않음
- 예를 들어 7개를 추첨하면 총 8회의 입력으로 완료
  - 1회: 7개 릴 전체 회전 시작
  - 2~8회: 1번부터 7번 릴까지 각각 실시간 번호 추첨 및 공개
- 한 릴이 감속 중일 때 추가 입력은 무시해 중복 정지를 방지
- 마지막 릴이 정지하면 추첨 완료
- 완료된 실제 추첨 번호 자체는 자동 번호 발급 기록에 섞어 저장하지 않음

## 자동 발급 번호 확인

실시간 추첨이 끝나면 `자동 번호 발급` 탭에서 만들어 둔 기록 전체를 실제 추첨 결과와 자동 비교합니다.

- 보너스 번호가 없으므로 등수 강제 판정 대신 `N개 일치`로 표시
- 각 기록에서 일치한 번호 강조
- 확인한 기록 수, 최고 일치 개수, 하나 이상 일치한 기록 수 표시
- `다시 확인`으로 현재 자동 발급 기록을 재검사 가능
- 자동 번호 발급을 추가한 뒤 추첨기 탭으로 돌아오면 현재 실시간 추첨 결과 기준으로 다시 비교

## 외부 번호 일괄 확인

- 여러 조합을 한 줄에 한 조합씩 붙여넣어 현재 실시간 추첨 결과와 일괄 비교
- 각 번호는 공백·쉼표·세미콜론으로 구분 가능
- `발급 기록 넣기`로 현재 자동 발급 기록을 입력 영역에 바로 불러올 수 있음
- 실시간 추첨이 완료된 뒤 검사 가능

## 이미지와 레이어

Base64나 별도 이미지 로더를 사용하지 않습니다.

```text
assets/Frame2.png
```

릴 Canvas는 PNG 프레임보다 낮은 `z-index`에 배치되어 입 구멍을 통해서만 보입니다. 페이지와 이미지가 축소·확대되어도 입 위치와 릴 크기는 비율로 함께 조정됩니다.

## 구조

```text
.
├─ index.html
├─ styles.css
├─ draw-modes.css
├─ live-draw.css
├─ roulette-core.js
├─ roulette-draw.js
├─ live-draw.js
├─ roulette-ui.js
├─ .nojekyll
└─ assets/
   └─ Frame2.png
```

## 실행

별도 빌드 과정이 없습니다.

```bash
python -m http.server 8080
```

또는 GitHub Pages에서 `main` / `/ (root)`를 배포 대상으로 지정하면 바로 실행할 수 있습니다.
